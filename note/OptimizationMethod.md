## 优化策略
- 获取数组`int a[n]`大小时用`sizeof(a)`而不用`sizeof(int) * n`。前者是编译时处理，后者是运行时
- 写读内存会耗时间，要注意控制访问的时机，避免无用的访存
- 函数内联
- 相同空间复杂度的情况下，更倾向用变量(堆)存储数据而非用递归
   - 在许多语言中，堆栈帧的开销很大(如 Python)
   - 最大的运行时堆栈深度有限，虽然可以增加，但是有可能导致底层解释程序内存出错，这限制了算法能处理的数据量